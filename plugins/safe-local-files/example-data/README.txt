This directory is intentionally safe sample data.

Change the root field in your private config.json to the exact directory that
ChatGPT or Codex may read. The server cannot access paths outside that root.
